name: uptimekuma

services:
  uptime-kuma:
    image: louislam/uptime-kuma:2.4.0
    volumes:
      - uptime-kuma-data:/app/data
    ports:
      - 3001:3001
    restart: unless-stopped

volumes:
  uptime-kuma-data:
